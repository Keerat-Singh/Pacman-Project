import torch

# Path to your model file
model_path = 'Model/Model_at_episode_0'

# Load the file to inspect its contents
model_data = torch.load(model_path, map_location='cpu')

# Print the keys of the dictionary
print("Keys in the pickle file:", model_data.keys())

# Print the contents of the dictionary
for key, value in model_data.items():
    print(f"Key: {key}, Value: {value}")

# agent = None
# model_name = "pacman_dqn_interrupted.pth"
# env = PacmanGame()
# agent = load_model(agent, model_name, env)

# checkpoint = torch.load(model_path, map_location='cpu')  # Ensure compatibility
# # agent.policy_net.load_state_dict(checkpoint['model_state_dict'])
# episode_count = checkpoint['episode_count']